<UserControl
    x:Class="Mojio.Platform.SDK.SampleApp.Controls.VehicleSummary"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mojio.Platform.SDK.SampleApp.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid MaxWidth="200">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
        </Grid.ColumnDefinitions>
        <Image Source="{Binding Image.Normal}" Width="200" Grid.Row="0" Grid.Column="0" />
        <TextBlock Text="{Binding Name}" Grid.Row="1" Grid.Column="0" />
        <TextBlock Text="{Binding VIN}" Grid.Row="2" Grid.Column="0" />
        <TextBlock Text="{Binding Selected}" Grid.Row="3" Grid.Column="0" />

        <TextBlock Text="asdfasdfasdfasdf" Grid.Row="4" Grid.Column="0" />
    </Grid>
</UserControl>